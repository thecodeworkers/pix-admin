import { DispatchProps } from '../../types';

const initialState = {};

export default (state = initialState, { type, payload }: DispatchProps) => {
  switch(type) {
    

    default:
    return state
  }
}
